feat(ux): add read-only field visual indicators

Implement read-only field visual feedback in ConfigFieldRenderer:

**Visual Indicators:**
- Lock icon displayed next to field label when read-only
- Reduced opacity (0.7) on read-only fields for visual de-emphasis
- Background color changed to theme.surface for read-only inputs
- Touch targets remain accessible size (important for maritime UI)

**Implementation:**
- Extract label and lock icon into flexRow layout
- Apply conditional styling based on isReadOnly state
- Uses UniversalIcon component for consistent cross-platform lock icon
- Maintains glove mode support (48-56px touch targets unchanged)

**User Experience Impact:**
- Users instantly recognize which fields cannot be edited
- Prevents confusion when attempting to interact with locked fields
- Lock icon provides clear affordance (universal "do not touch" indicator)
- Consistent with maritime instrument UI patterns

**Testing:**
- TypeScript compilation: ✅ No errors
- All field types updated: textInput, numericInput
- Read-only detection logic preserved (readOnly, readOnlyIfValue states)
- Theme integration verified (uses theme.surface, theme.textSecondary)

**Files Modified:**
- ConfigFieldRenderer.tsx: +30 lines (lock icon UI, read-only styling)