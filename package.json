{
  "name": "bmad-autopilot-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "BMad Autopilot - React Native marine instrument display. Main app is in boatingInstrumentsApp/",
  "scripts": {
    "android": "cd boatingInstrumentsApp && npm run android",
    "ios": "cd boatingInstrumentsApp && npm run ios",
    "web": "cd boatingInstrumentsApp && npm run web",
    "test": "cd boatingInstrumentsApp && npm test"
  }
}
