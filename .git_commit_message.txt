feat(sensor-config): add threshold value labels below slider thumbs

Enhancement:
- Display threshold values directly below each thumb
- Show label (Warning/Critical) above thumb
- Display formatted value below thumb
- Show formula hints (→ computed value) for ratio mode

Implementation:
- Updated renderThumb to return View with 3 elements:
  1. Label above thumb (Warning/Critical)
  2. Thumb circle (colored dot)
  3. Value container below thumb (formatted value + hint)
  
- Added styles for label positioning:
  * thumbLabel: Base label style with absolute positioning
  * thumbLabelTop: 28px below thumb (above circle)
  * thumbLabelBottom: 28px above thumb (below circle)
  * thumbValueContainer: Container for value + formula hint
  * thumbLabelComputed: Smaller font for computed hints

User Experience:
- Easier to read threshold values while dragging
- No need to look at top legend during adjustment
- Formula hints show computed absolute values (ratio mode)
- Color-coded labels match thumb colors (orange/red)

Example:
  Warning (above thumb)
     🟠 (orange thumb)
  8.2 ft (below thumb)
  → 2.5 m (formula hint if ratio mode)

Files: AlarmThresholdSlider.tsx