refactor(sensors): complete useSensorConfigForm unification

Phase 1: Data Structure Unification - Step 5 Complete

Removed all requiresMetricSelection conditionals from useSensorConfigForm:
- alarmFormula: Always use watchedMetric || defaultMetric
- ratioUnit: Always use watchedMetric || defaultMetric
- sliderPresentation: Always use watchedMetric || defaultMetric
- metricLabel: Always use watchedMetric || defaultMetric
- currentMetricValue: Always use watchedMetric || defaultMetric

Pattern (before):
```typescript
const metricKey = requiresMetricSelection ? watchedMetric : alarmFieldKeys[0];
if (requiresMetricSelection && watchedMetric) {
  // Multi-metric path
} else {
  // Single-metric path
}
```

Pattern (after):
```typescript
const defaultMetric = alarmFieldKeys[0];
const metricKey = watchedMetric || defaultMetric;
// Single unified path - works for both
```

Impact:
- ~100 lines of conditional logic eliminated
- requiresMetricSelection now UI-only (disable dropdown)
- All computed values use unified metric resolution
- Zero branching based on sensor alarm field count

Next: Simplify SensorConfigDialog rendering (remove conditional MetricSelector)

Files: src/hooks/useSensorConfigForm.ts
