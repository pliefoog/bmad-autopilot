refactor(sensors): simplify useSensorConfigForm - unified metric access

Phase 1: Data Structure Unification - Step 3-4 Progress

Changes:
- Simplified metric key resolution in useSensorConfigForm.ts:
  - Added defaultMetric constant (alarmFieldKeys[0])
  - Removed dual-path logic for single vs multi-metric
  - Always use watchedMetric || defaultMetric (no conditionals)
  - Simplified sound pattern loading (always from metrics object)

- Updated SensorInstance.ts:
  - Removed single-metric path in updateThresholdsFromConfig
  - Schema V4: Always read from config.metrics object

Before (conditional logic):
```typescript
const metricKey = requiresMetricSelection ? watchedMetric : alarmFieldKeys[0];
if (requiresMetricSelection && firstMetric) {
  // Multi-metric path
} else {
  // Single-metric path  
}
```

After (unified):
```typescript
const defaultMetric = alarmFieldKeys[0];
const metricKey = watchedMetric || defaultMetric;
// Always use metrics object
if (firstMetric && currentThresholds?.metrics?.[firstMetric]) {
  // Single unified path
}
```

Benefits:
- Removed ~50 lines of conditional logic
- Single code path for metric resolution
- requiresMetricSelection still tracked (for UI only - disable dropdown)
- Type-safe with defaultMetric constant

Remaining in Phase 1:
- Remove more requiresMetricSelection conditionals in computed values
- Simplify SensorConfigDialog rendering (always show MetricSelector)
- Update schemaDefaults.ts if needed

Files: src/hooks/useSensorConfigForm.ts, src/types/SensorInstance.ts
