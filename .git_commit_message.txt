refactor: modernize SensorConfigDialog with card-based layout and platformTokens

Comprehensive cleanup following Jan 2025 Settings Dialog UI Language requirements:

Architecture improvements:
- Card-based layout with proper visual separation (sensor selection, config, alarms)
- Platform-specific shadows (iOS shadowOpacity, Android elevation, web boxShadow)
- Consistent 20px padding and 16px marginBottom across all cards

Theme integration:
- Added getPlatformTokens() for typography, borderRadius, shadows
- Replaced hardcoded values with platformTokens references throughout
- Consistent fontFamily usage across all text elements

Typography hierarchy:
- sectionTitle: 18px, 700 weight for card headers ("Sensor Selection", "Alarms")
- settingLabel: 16px, 600 weight for setting names
- groupLabel: 14px, 600 weight for subsection labels ("Alarm metric", "Threshold values")
- helpText: Uses platformTokens.typography.hint (11px, italic, secondary color)

UI patterns:
- settingRow: Horizontal layout for "Enable alarms" toggle
- settingGroup: Container for nested controls when alarm enabled
- inputWrapper: Proper spacing for picker controls
- Minimalistic information hierarchy (no redundant labels)

Benefits:
- Consistent with other dialogs (UnitsConfigDialog, ConnectionConfigDialog)
- Better visual separation with card shadows
- Improved readability with proper typography scale
- Platform-appropriate styling (iOS/Android/web differences)
- Cleaner codebase with centralized styling

Files: SensorConfigDialog.tsx (1215 lines)
