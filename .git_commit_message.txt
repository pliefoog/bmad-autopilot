fix(sensor-config): restore proper visual styling for threshold slider

Problem:
- No green/blue slider rail visible (full width)
- Critical thumb not red (was blue)
- Warning thumb not orange (was blue)
- All thumbs used theme.primary (blue) color

Root Cause:
- renderThumb/renderRail/renderRailSelected were inline arrow functions
- Didn't utilize RangeSlider's name parameter ('low'/'high')
- Couldn't differentiate between warning and critical thumbs
- Missing useCallback import

Solution:
- Import useCallback from React
- Create memoized render functions (renderThumb, renderRail, renderRailSelected)
- Use name parameter in renderThumb to determine warning vs critical
- Apply correct colors: theme.warning (orange), theme.error (red)
- Fix spacing.section → spacing.lg (section doesn't exist in settingsTokens)

Visual Result:
- Warning thumb: Orange (theme.warning)
- Critical thumb: Red (theme.error)
- Rail (unselected): Gray (theme.border)
- Rail (selected): Blue (theme.primary)
- Full-width slider rail visible
- Thumbs properly colored based on direction

Implementation:
- renderThumb checks if thumb is 'low' or 'high'
- For 'above' direction: low=warning, high=critical
- For 'below' direction: low=critical, high=warning
- Memoized with useCallback for performance

Files: AlarmThresholdSlider.tsx