fix: use getSensorSchema helper instead of direct SENSOR_SCHEMAS access

Replaced direct SENSOR_SCHEMAS access with getSensorSchema() helper function
for better encapsulation and consistency with rest of codebase.

Changes:
- _recalculateFormulaDependentThresholds(): Use getSensorSchema(this.sensorType)
- updateThresholdsFromConfig(): Use getSensorSchema(this.sensorType)

Benefits:
- Consistent API usage across codebase
- Better encapsulation of schema access
- Leverages existing helper function
- No functional change, pure refactoring

Files: SensorInstance.ts (2 occurrences fixed)
