refactor: remove deprecated functions and optimize SensorConfigDialog

Dead code cleanup and performance optimizations from senior architect review:

SensorConfigRegistry.ts - Removed deprecated functions:
- Removed getSmartDefaults() (deprecated wrapper, only used in example comment)
- Removed shouldShowField() (always returned true, no actual logic)
- Updated example comment to use getAlarmDefaults() directly
- Lines saved: ~30 lines of dead code

SensorConfigDialog.tsx - Optimizations and cleanup:
- Removed shouldShowField import and usage (function removed from registry)
- Fixed double-call of renderConfigFields() by memoizing result
- Changed conditional from {renderConfigFields() && ...} to {configFieldsJSX && configFieldsJSX.length > 0 && ...}
- Prevents calling expensive render function twice (was called for check + render)
- More robust empty state check (array length > 0 instead of truthy)

Performance impact:
- Eliminated redundant field rendering (2x → 1x per form data change)
- Removed unnecessary conditional check function (shouldShowField)
- Cleaner dependency arrays in useCallback/useMemo

Code quality improvements:
- Removed backward compatibility layer that provided no value
- More explicit empty state handling with length check
- Consistent with React best practices (memoize expensive computations)

Files: SensorConfigRegistry.ts, SensorConfigDialog.tsx
